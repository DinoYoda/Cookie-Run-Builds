/** Copy to tier-feedback-config.local.js (gitignored) after deploying the worker. */
window.TIER_FEEDBACK_CONFIG_LOCAL = {
    submitUrl: "https://crk-tier-feedback.YOUR_SUBDOMAIN.workers.dev",
    listUrl: "https://crk-tier-feedback.YOUR_SUBDOMAIN.workers.dev",
    adminToken: "YOUR_ADMIN_TOKEN",
}
