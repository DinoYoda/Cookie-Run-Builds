;(function () {
  const root = document.getElementById("treasuresContent")
  if (!root) return

  /**
   * Filled in later: registry in data, static list, or scan pictures/treasures.
   * For now, show a short placeholder.
   */
  root.innerHTML = "<p>Treasure list and write-ups will be wired up next (icons, names, effect text).</p>"
})()
